package com.bgasparotto.primeapi.config;

import com.google.inject.AbstractModule;

public class GuiceInjectorModule extends AbstractModule {
    @Override
    protected void configure() {
        // Explicit bindings go here.
    }
}
